@model IEnumerable<Supermarket.Models.Supermarket>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SupermarketName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SupermarketName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SupermarketId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SupermarketId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SupermarketId })
        </td>
    </tr>
}

</table>
